export enum Gender {
  UNKNOWN = "Unknown",
  MALE = "Male",
  FEMALE = "Female",
}

export enum Interest {
  PROJECT = "Project",
  TEAM = "Team",
  RELATIONSHIPS = "Relationships",
  KNOWLEDGE = "Knowledge",
}

export enum Role {
  USER = "User",
  ADMIN = "Admin",
}
